source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in errors_savior.gemspec
gemspec

group :development, :test do
  gem 'sqlite3'
  gem 'puma', '~> 5.2'
  gem 'simplecov'
  gem 'rspec-rails'
  gem 'bootsnap', '>= 1.1.0'
  gem 'byebug'
  gem 'database_cleaner'
  gem 'factory_bot'
  gem 'faker'
  gem 'rubocop'
  gem 'rubocop-rspec'
  gem 'generator_spec'
end
